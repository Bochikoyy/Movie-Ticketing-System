#ifndef PAYMENTS_H
#define PAYMENTS_H

// Prototypes
float calculateTotal(int ticketCount); // With param / With return
int processPayment(float totalAmount); // With param / With return

#endif
